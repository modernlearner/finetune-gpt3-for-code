{
  "name": "finetune-gpt3-for-code",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker:build": "docker build -t finetune-gpt3-for-code .",
    "docker:run": "docker run -it --rm --log-driver none --env-file .env -v $(pwd)/data:/data finetune-gpt3-for-code"
  },
  "author": "Rudolf Olah <rudolf.olah.to@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "csv-parse": "^5.3.3",
    "dotenv": "^16.0.3",
    "openai": "^3.1.0",
    "typescript": "^4.9.4",
    "yargs": "^17.6.2"
  }
}
